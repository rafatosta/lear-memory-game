# Orientação para contribuidores

Preserve a separação entre `domain` (regras puras), `content` (dados editáveis), `services` (infraestrutura) e componentes. Não coloque caminhos de assets, preços ou textos educativos em componentes visuais. Execute `npm run validate` antes de enviar mudanças.
