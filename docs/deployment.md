# Deploy

O workflow `deploy-pages.yml` publica o build em GitHub Pages quando uma release é publicada. O disparo manual (`workflow_dispatch`) continua disponível. Ative GitHub Pages com fonte GitHub Actions nas configurações do repositório.
